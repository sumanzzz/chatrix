{
  "name": "chatrix",
  "version": "1.0.0",
  "description": "Real-time anonymous chatrooms",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "backend": "cd backend && npm run dev",
    "frontend": "cd frontend && npm run dev",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": ["chat", "realtime", "anonymous", "socket.io", "webrtc"],
  "author": "Chatrix Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
